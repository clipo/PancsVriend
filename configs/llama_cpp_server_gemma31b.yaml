# llama.cpp server config — Gemma 4 31B Q5_K_M, port 8081
# NOTE: only used by the LEGACY single-stream `python -m llama_cpp.server`.
# run_gemma31b.sh now launches the native `llama-server` (continuous batching) and
# does NOT read this file — set slots/ctx/ngl via the vars at the top of that script.
host: "0.0.0.0"
port: 8081

models:
  - model: "/srv/shared/schelling/PancsVriend/llms/gemma-4-31B-it-Q5_K_M.gguf"
    model_alias: "gemma-4-31b-q5"
    n_gpu_layers: -1
    flash_attn: true
    n_ctx: 2048
    n_batch: 512
